position: fixed는 해당 tag가 스크롤을해도 계속 같은 자리에 머무르게 하고싶을때 사용
또한 fixed될때의 positiob(좌표)는 초기에 설정해둔 좌표이다.
따라서 스크롤을 한다고해서 새로운 좌표로 계속 갱신되는건 아닌거 같다.

div를 두개 만들고 위아래로 나란히 있는 상태에서 top bottm left right중 하나라도 수정하면
box의 위치가 변하게되고, 이러면 동일한 레이어가 아니게되버린다.

여튼 position: fixed를 하게되면, 엑셀에서 틀고정처럼 가장 최상단(화면 가장 앞)에 위치하게된다.